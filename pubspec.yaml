name: restaurant_unified_app
description: "Unified Restaurant Management App - Admin & Staff"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  go_router: ^13.0.0
  google_fonts: ^6.2.0
  flutter_animate: ^4.5.0
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  qr_flutter: ^4.1.0
  printing: ^5.12.0
  pdf: ^3.10.8
  path_provider: ^2.1.5
  fl_chart: ^0.66.2
  external_path: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Add assets if any. I'll check both projects for assets.
